export const tpl: string = `

  <p class="profile-form__title">{{title}}</p>
  <input
    type="{{type}}"
    class="profile-form__value"
    value="{{value}}"
    name="{{formName}}"
    placeholder="{{title}}"
  />
`;
